
<!-- Google Tag Manager with Consent Mode V2 -->
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag() { dataLayer.push(arguments); }

  const consentState = {
    ad_storage: 'denied',
    analytics_storage: 'denied',
    ad_user_data: 'denied',
    ad_personalization: 'denied',
    wait_for_update: 500
  };

  try {
    const saved = localStorage.getItem('cookiePreferences');
    if (saved) {
      const prefs = JSON.parse(saved);
      if (prefs.analytics) {
        consentState.analytics_storage = 'granted';
      }
      if (prefs.marketing) {
        consentState.ad_storage = 'granted';
        consentState.ad_user_data = 'granted';
        consentState.ad_personalization = 'granted';
      }
    }
  } catch (e) {}

  gtag('consent', 'default', consentState);

  // Defer the heavy GTM container so it never competes with the map/filters
  // for the main thread during initial load. The consent signal above stays
  // inline/early; only gtm.js is deferred until after the page is interactive.
  (function (w, d, s, l, i) {
    var loaded = false;
    function loadGTM() {
      if (loaded) return;
      loaded = true;
      w[l] = w[l] || [];
      w[l].push({ 'gtm.start': new Date().getTime(), event: 'gtm.js' });
      var f = d.getElementsByTagName(s)[0],
        j = d.createElement(s),
        dl = l != 'dataLayer' ? '&l=' + l : '';
      j.async = true;
      j.src = 'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
      f.parentNode.insertBefore(j, f);
    }

    // Load when the browser is idle after the page has finished loading, so the
    // map and filters win the main thread first. Falls back to a short timeout
    // to keep the bounce window (missed pageviews) small.
    function scheduleLoad() {
      if ('requestIdleCallback' in w) {
        w.requestIdleCallback(loadGTM, { timeout: 3000 });
      } else {
        w.setTimeout(loadGTM, 1500);
      }
    }

    // Also load on the first user interaction. This guarantees GTM is present
    // before the user can reach the "Buy it Now" CTA (an outbound, same-tab
    // link whose click fires the Google Ads conversion), closing the race
    // where a very fast click would navigate away before GTM had loaded.
    var interactionEvents = ['pointerdown', 'keydown', 'scroll', 'touchstart'];
    function onFirstInteraction() {
      interactionEvents.forEach(function (ev) {
        w.removeEventListener(ev, onFirstInteraction, { capture: true });
      });
      loadGTM();
    }
    interactionEvents.forEach(function (ev) {
      w.addEventListener(ev, onFirstInteraction, { capture: true, passive: true });
    });

    if (d.readyState === 'complete') {
      scheduleLoad();
    } else {
      w.addEventListener('load', scheduleLoad);
    }
  })(window, document, 'script', 'dataLayer', 'GTM-T948MNX');
</script>
