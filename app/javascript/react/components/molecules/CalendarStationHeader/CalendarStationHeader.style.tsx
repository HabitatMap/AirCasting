import styled from "styled-components";
import media from "../../../utils/media";

const TextContainer = styled.div`
  display: flex;
  flex-direction: column;
  text-align: left;
`;

const BoldText = styled.div`
  font-weight: 600;
  display: inline;
`;

export { TextContainer, BoldText };
