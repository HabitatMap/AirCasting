import { RootState } from ".";

// export const selectSessions = (state: RootState) => state.sessions.sessions;
// export const selectFetchableSessionsCount = (state: RootState) => state.sessions.fetchableSessionsCount;
// export const selectSessionsStatus = (state: RootState) => state.sessions.status;
// export const selectSessionsError = (state: RootState) => state.sessions.error;
